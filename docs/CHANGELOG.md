# Changelog

All notable changes to the Vision Constructions website. Dates are in `YYYY-MM-DD` format where available.

---

## Approved changes

| Date | Change |
|---|---|
| 2026-04-23 | **Nav bar no longer shifts between pages.** Added `scrollbar-gutter: stable` to the `html` rule in `styles.css`. The fixed-position nav was shifting ~8px horizontally when moving from the homepage to subpages because the homepage sometimes rendered without a scrollbar while subpages always had one — the ~15–17px scrollbar width changed the viewport width, pulling the centered nav inner with it. Reserving scrollbar space on every page keeps the viewport width constant, so the logo and links stay pinned in exactly the same spot everywhere. |
| 2026-04-23 | **Homepage hero headline reverted.** Briefly changed `Built on values.` → `Driven on values.` to test the auto-deploy pipeline end-to-end (GitHub push → Vercel build → live site). Verified working, reverted to `Built on values.` |
| 2026-04-21 | **AIMS client added to Jetty Maintenance project card.** Added a `Client: Australian Institute of Marine Science` meta line to the Jetty Maintenance & Upgrades card on the projects page, alongside the existing Location and Completed fields. First project card to re-introduce client attribution since the earlier sweep stripped all client names — driven by AIMS project sheet being the first real client data approved for public display. |
| 2026-04-21 | **Brendan Rosser restored to team section.** Added Brendan's card back onto about.html alongside Travis Maher and Jack Willcox. Team now shows 3: Travis (MD), Jack (Engineering & BD), Brendan (Supervisor). |
| 2026-04-21 | **About page — employee count and lost-time injuries claim removed.** Dropped "16 direct employees" from the Our Team stat (now just reads "Experienced site supervisors, qualified tradespeople & operators, Townsville-based") and dropped "Zero lost-time injuries since founding" from the Safety Record stat (now "ISO 45001 WHS system · QLD WHS Act compliant · regular audits"). Keeps the site free of specific headcount and injury-record claims. |
| 2026-04-21 | **Policies page — real QSERC ISO badges replacing text tiles.** The three ISO certification tiles (45001, 14001, 9001) now display the official QSERC-issued logo badges (blue/green/red pill graphics with "QSERC" + ISO number) instead of plain text. New CSS class `.v2-feature-icon-img` handles scaling at 64px height, left-aligned, transparent background. Local Buy and WHS QLD tiles still use the text badge style (no image badges provided for those). Source AI/EPS/PDF files archived to `_source/logos-raw/qserc-iso-badges/`. |
| 2026-04-21 | **Jetty Guide Pile Repairs photo — grey pillarbox stripped.** The previous version of `jetty-guide-pile-welding.jpg` had baked-in grey bars on the left and right (the original was portrait, padded out to 16:10 with solid grey). Cropped those off so the photo now fills the card cleanly via `object-fit: cover`. Small quality loss since the usable content was only 467×492 — upscaled to 900px wide for display clarity. |
| 2026-04-21 | **Chemical Dosing Skids project photo updated.** New site photo showing the completed dosing installation — FRP grating platform, canopy, dosing skids on the right, and safety handrails. Replaces the previous FRP platform shot on the projects page card. Saved as `chemical-dosing-skids.jpg` (1600×1000, 319KB, cropped from the portrait WhatsApp original). |
| 2026-04-21 | **Homepage Proof grid — "0 lost time injuries" stat removed.** The homepage Safety/Quality/Environmental proof section no longer displays the "0 Lost time injuries since we started in 2009" stat. Grid now shows three tiles: 15+ Years · 200+ Projects · ISO (9001/14001/45001). |
| 2026-04-21 | **Careers page — two live roles advertised.** Replaced the "No Current Vacancies" empty-state panel with two active role cards: **Skilled Tradespeople** (concreters, formwork carpenters, boilermakers) and **Labourers**. Section heading updated from "Currently advertising" to "Join our team" with the "Vision Constructions QLD is growing" lede. Each card has an Apply → button scrolling to the application form. |
| 2026-04-21 | **Team section pared back on about page.** Removed three team member cards: Brendan Rosser (Supervisor), Maree Surman (Administration & Project Support), and Kylie Maher (Administration). Team section now shows Travis Maher (Managing Director) and Jack Willcox (Engineering & Business Development Manager) only. |
| 2026-04-20 | **Homepage hero photo updated — crane lifting service pontoon.** New Vision site photo on the homepage hero: a mobile crane with Vision crew in hi-vis lifting a green-decked marine service pontoon into the water at a remote coastal site (blue sky, mountains, mangroves). Saved as `hero-crane-pontoon.jpg` (1800×1200, 462KB, optimised from the original 1536×1024 PNG). Replaces the previous `fleet-stormy.jpg` hero, which remains in use on the capabilities page and the Infrastructure Support capability tile. Original PNG archived to `_source/photos-raw/`. |
| 2026-04-20 | **Capability Statement preview — manual page turn with arrows.** Replaced the hover-autoplay book-flip with user-controlled prev/next arrow buttons and a `1 / 11` page counter underneath the preview. Left/right arrow keys also work when the preview is focused. The carousel **loops** — clicking next on page 11 springs back to page 1 (all pages reset at once), and clicking prev on page 1 jumps to page 11 (all pages flip forward). Visitors can flick through at their own pace instead of being rushed by the autoplay cycle. |
| 2026-04-20 | **Contact + careers forms now send real emails.** Homepage enquiry, contact page, and careers application forms all wired to Formspree endpoint `https://formspree.io/f/mbdqkanq` (free plan, 50 submissions/month, all three forms share one endpoint). Submissions go to `admin@visionconstructions.com.au`. Subject lines distinguish each form: *"New homepage enquiry"*, *"New contact enquiry"*, *"New careers application"*. Careers form has `enctype="multipart/form-data"` so resume attachments come through. `main.js` `wireForm()` handler shows real sending → success / error states based on actual HTTP response. |
| 2026-04-20 | **Certification badges no longer cramped on policies page.** The five label tiles (ISO 45001, ISO 14001, ISO 9001, Local Buy, WHS QLD) were being squeezed into a fixed 52×52 `.v2-feature-icon` square with 0.95rem text. Added a new `.v2-feature-badge` modifier that auto-sizes width, pads to 14×22px, lifts the font to 1.15rem, and left-aligns — text now breathes and reads cleanly. Inline style hacks replaced on all 5 tiles. |
| 2026-04-20 | **Project card slideshow reverted — back to single photo per card.** Removed the dual/slideshow setup on Culvert, Sludge Lagoon, and Jetty Guide Pile Repairs. Cards now show only their original primary photo again. Secondary photos (`culvert-laroona-2.jpg`, `sludge-trench-excavator.jpg`, `jetty-fleet-on-deck.jpg`) deleted, `.v2-slideshow` CSS rules removed, and the auto-cycle JS block removed from `main.js`. |
| 2026-04-20 | **Client names removed from projects page.** All six project cards had their client names stripped: project titles ("— AIMS", "— TRILITY", "— JPD Constructions") removed, the "Client:" meta rows deleted, body copy generalised ("AIMS service pontoon" → "marine research service pontoon"; "AIMS STP" → "STP"; "Douglas WTP" → "a water treatment plant"), image alt text cleaned, and the subhero lede's client roster ("for AIMS, TRILITY, Glencore, Townsville City Council, and more") changed to sector descriptors ("for government, water utility, resources, and marine science clients"). Locations and completion dates kept. |
| 2026-04-20 | **New capability tile photos — Infrastructure Support + SMP.** Two real Vision site photos placed on the homepage capability grid. Infrastructure Support now shows a Vision crew member in hi-vis + Vision hardhat servicing water-tank filters at a WTP (`support-filter-servicing.jpg`, 1800×1350). SMP now shows a Vision welder performing structural steel modifications with a Vision-branded hardhat in the foreground (`smp-welder-steel-mods.jpg`, 1800×1350 — cropped from portrait original to 4:3, GPS/caption strips removed). Raw originals archived to `_source/photos-raw/`. |
| 2026-04-20 | **Lightened grey palette for higher contrast and easier reading.** The stone/concrete theme stays but every light-bg token got lifted: `--h-bg` from `#C4C3BF` → `#E4E2DC`, `--h-alt` from `#B1B0AC` → `#D4D2CC`, `--h-card` from `#D6D5D1` → `#F1EFEA`, `--h-border` from `#9D9C98` → `#B8B6B0`. Cascades through body, sections, cards, panels, form inputs, feature icons, job tags, and project detail cards across all 7 pages — dark text now sits on lighter surfaces for a clearer, less muddy read. Capability tiles on the homepage (already explicitly white) still distinguish from the new lighter page bg. |
| 2026-04-20 | **Fixed invisible ISO paragraph on homepage Proof section.** The lead paragraph inside the dark `.v2-proof` block ("Our integrated management systems are fully ISO-accredited…") was inheriting the darkened `--h-ink-muted` from the site-wide readability pass, rendering dark text on a dark background. Added a `.v2-proof .v2-lead` override to white-at-0.92 alpha so it reads clearly. |
| 2026-04-20 | **Site-wide text readability pass.** All body text, kickers, labels, and leads now use higher-contrast ink values so text stops looking muddy or washed out against the grey backgrounds. Changes: (1) `--h-ink` darkened from `#17161A` → `#121013`, `--h-ink-muted` from `#404040` → `#1F1D1A`, `--h-ink-soft` from `#6B6A66` → `#3A3834` — the soft token fixes an AA contrast failure used in every kicker/label. (2) Legacy `--grey-600`/`--grey-400` tokens (footer + nav on dark bg) lifted from `#7A7A7A`/`#AAAAAA` to `#B2B0AC`/`#D8D6D2` so footer copy and dark-section grey text read cleanly. (3) All white-on-dark text bumped from 0.6–0.8 alpha to 0.85–0.96 alpha (hero lede, subhero lede, CTA band, proof stats, dark features, project card copy, breadcrumbs, eyebrows). (4) Line-heights bumped 1.55 → 1.65/1.7 on leads, cap body, feature body, project detail body, timeline, checklist, panel body, job cards — text breathes better and is easier to scan. Font-size nudges of +0.02–0.03rem on leads/body where text felt tight. Cascades across all 7 pages via shared styles. |
| 2026-04-20 | **Homepage capability tiles set to white.** The four "Explore →" capability tiles (Infrastructure Support, Marine & Water, SMP, Civil Construction) now have a white card background instead of the previous light grey `#D6D5D1` — stronger contrast against the page background and imagery. |
| 2026-04-20 | **Contact email restored to admin address.** All 7 HTML pages now point to `admin@visionconstructions.com.au` (nav, contact details, forms, footers, and JSON-LD structured data) — swapped back from the temporary personal email. |
| 2026-04-20 | **Sludge Lagoon photo replaced with clearer version.** Same framing (two excavators shaping the lagoon cells) but much sharper and brighter. Flows through to the projects page card and the homepage featured tile. Orphaned earlier versions (`project-sludge-lagoon.jpg`, `sludge-lagoon-crew.jpg`) deleted from the images folder. |
| 2026-04-20 | **Homepage hero photo updated.** New hero image — Vision-branded trailer, truck, and crane mobilised to a coastal worksite under a storm sky. Saved as `fleet-stormy.jpg` (1600×1200, cleaned/resized from PDF source). The old `fleet-coastal.jpg` remains in use on the capabilities page and the Infrastructure Support capability tile. |
| 2026-04-20 | **QBCC + QLD Govt prequal removed + headcount corrected.** QBCC Builder's Licence and QLD Government Prequalified tiles both removed from policies page (Vision doesn't hold either) — certifications row is now 3 cards (ISO 45001/14001/9001) and the compliance row is now 2 cards (Local Buy, WHS QLD) centered. About page blurb updated: QBCC + prequal references removed, headcount corrected from ~27 to 16 direct employees. |
| 2026-04-20 | **Nav logo bumped up.** Logo height increased from 42px to 56px in the navigation bar on every page — brand reads noticeably stronger. |
| 2026-04-20 | **Trust strip removed from homepage.** The full client/partner logo section (label + 9 logo tiles — AIMS, TRILITY, Glencore, APLNG, Townsville City Council, QLD Government, QLD Transport & Main Roads, Sun Metals, MSF Sugar) has been deleted from `index.html`. Logo files remain in `images/client-logos/` in case they're needed later. |
| 2026-04-20 | **Civil Construction panel photo changed.** Now shows a grader and roller forming a remote road — strongly matches the road construction / NDRRA flood restoration scope listed on the panel. |
| 2026-04-20 | **Capability Statement preview turns pages on hover (book-flip).** 11-page preview stacked with CSS 3D perspective — hover to see each page physically flip left like a real book, exposing the next page beneath. Mouse-off resets the stack. |
| 2026-04-20 | **Guide Pile Repairs photo updated.** New close-up of a welder seal-welding the stainless flat bar around a pile — exactly the scope described on the card. Swapped in on the projects page. |
| 2026-04-20 | **AIMS jetty photos swapped on projects page.** The crew-with-guide-pile photo now sits on the Jetty Maintenance & Upgrades card; the service-pontoon photo moved to the Jetty Guide Pile Repairs card. |
| 2026-04-20 | **Facebook link added to footer.** New Facebook icon alongside LinkedIn in the footer social row on every page, linking to `facebook.com/p/Vision-Constructions-Queensland-Pty-Ltd-100090732505357`. |
| 2026-04-20 | **Sludge Lagoon photo updated.** New site photo showing excavators shaping the lagoon cells — swapped in on both the projects page card and the featured homepage project tile. |
| 2026-04-20 | **STP project photo updated.** Replaced the drone hopper shot with a new crew photo showing a concrete pour at sunrise (pump boom + crew) — used on both the projects page STP card and the matching homepage project tile. The old drone image still serves as the capabilities page hero. |
| 2026-04-20 | **New project card added — Culvert Installation (JPD Constructions, 2025).** Laroona Road culvert works at Hervey Range: reinforced concrete bases, cut-off walls, multi-cell culvert structures, sequential pours in a live road corridor. Photo extracted from the JPD project sheet PDF and placed at the top of the projects grid (newest first). |
| 2026-04-20 | **Folder restructure + documentation.** Root cleaned to site-serving files only. `_source/` added for raw originals (photos, legacy logos, working docs, OneDrive sync). `docs/` added with CONTENT, BRAND, DEPLOY, CHANGELOG guides. `assets/` added — Capability Statement PDF moved there and link updated in `capabilities.html`. README rewritten as the "start here" document. |
| 2026-04-20 | **Two new photos placed.** `civil-concrete-pad.jpg` → Civil Construction capability panel (replacing excavator placeholder). `tank-access-overhead.jpg` → SMP capability panel (replacing scaffolded pipework placeholder). |
| 2026-04-20 | **Careers hero fix + about team polish.** Welder hero image `object-position` adjusted so the welder's head stays in frame. About team simplified to consistent initial circles (TM, JW, BR, MS, KM) — removed the mixed photo/initial treatment. |
| April 2026 | **V2 design system cascaded to all 6 subpages** (about, capabilities, projects, contact, careers, policies). New `v2-subhero`, `v2-panel`, `v2-timeline`, `v2-features`, `v2-jobs`, `v2-detail-list`, `v2-checklist` components. Dual-logo nav swap on scroll across site. |
| April 2026 | **Homepage rebuilt (v2 design):** light charcoal-grey palette, capability-statement copy, 7 real Vision photos placed across hero, capabilities, projects, why, and contact sections. |
| April 2026 | **Projects page updated with real content and photos** from 5 project sheets: Sludge Lagoon Extension (TRILITY 2024), Jetty Maintenance (AIMS 2023), STP Structural Concrete (AIMS 2022), Jetty Guide Piles (AIMS 2024), Chemical Dosing Skids (TRILITY 2025). Homepage project cards updated to match. |
| April 2026 | **README.md documentation created.** |
| April 2026 | **Initial site build** — 7 pages (index, about, capabilities, projects, careers, contact, policies), `styles.css`, `main.js`. |

---

## Pending / in progress

_None currently._

---

## How to update this file

When you make a change the client has approved, add a row at the **top** of the "Approved changes" table with today's date and a short description. Keep it plain English — no jargon. The description should tell a future reader **what** changed and **why**, not the technical details.
